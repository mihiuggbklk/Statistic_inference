# 中文讲稿：深度神经网络辅助的双重稳健估计

## 第1页：标题页

大家好，我今天报告的论文题目是《基于深度神经网络的双重稳健估计——结合非概率调查样本》。这篇论文来自 Dai 等人，2026 年发表在 arXiv 上。我将从研究动机、经典方法回顾、DNN方法创新、理论性质和数值结果几个方面来介绍这篇工作。

---

## 第2页：Motivation——整合两个不完美的数据源

首先来看研究动机。在现代调查统计中，我们常常面临两种数据来源。

左边是非概率样本 $S_A$。它包含了我们关心的结果变量 $y$ 和协变量 $x$，但它不是通过概率抽样获得的——样本的入样机制是未知的，可能存在自选择偏差。比如网络调查、自愿参与的问卷，都属于这一类。

右边是概率样本 $S_B$。它是通过已知的概率抽样设计获得的，因此具有代表性，有设计权重 $d^B$。但问题是，概率样本上往往没有我们关心的结果变量 $y$——它只有协变量 $x$。

我们的目标是什么呢？就是估计有限总体的均值 $\mu_y$。传统上，单独使用任何一种样本都有问题。所以很自然的想法就是：能不能把两种数据源整合起来，取长补短？

---

## 第3页：形式化设定与符号

现在我们把问题形式化。

有限总体 $U=\{1,\ldots,N\}$，目标参数是总体均值 $\mu_y$。

非概率样本 $S_A$ 大小为 $n_A$，我们观测到 $(x_i, y_i)$。定义入样指标 $R_i$ 表示第 $i$ 个个体是否进入非概率样本，入样概率记为 $\pi_i^A$。

概率样本 $S_B$ 大小为 $n_B$，我们观测到 $(x_i, d_i^B)$。其中 $d_i^B = 1/\pi_i^B$，$\pi_i^B$ 是一阶包含概率。注意，$S_B$ 中没有 $y_i$。

一个核心假设是可忽略性（ignorability）：入样概率 $\pi_i^A$ 只依赖于协变量 $x_i$，不依赖于结果变量 $y_i$。这意味着在给定 $x$ 的条件下，选择机制是随机的。这是所有后续方法的基础。

在这个框架下，有三种经典的估计策略：第一，建模结果变量 $y$ 和 $x$ 的关系，预测 $S_B$ 中的 $y$；第二，建模入样概率 $\pi^A$，对 $S_A$ 中的单元进行逆概率加权；第三，把两者结合起来，即双重稳健方法。

---

## 第4页：方法一——回归估计 REG

第一种方法是回归估计，也叫 mass imputation。

具体步骤是：首先，用非概率样本 $S_A$ 拟合一个结果模型 $m(x) = E(y|x)$，比如普通最小二乘；然后，用这个拟合的模型预测概率样本 $S_B$ 中每个单元的 $y$ 值；最后，用设计权重对预测值做加权平均。

这个估计量用到了概率样本的设计权重 $d_i^B$，保证了加权平均的代表性。

但关键假设是：结果模型 $m(x)$ 必须正确设定。如果真实的 $E(y|x)$ 包含交互项、阈值效应或非线性结构，而我们却用了线性模型，那么 $\hat\mu_{REG}$ 就是不一致的。无论设计权重有多好，都无法挽救一个错误设定的结果模型。

---

## 第5页：方法二——逆概率加权 IPW

第二种方法是逆概率加权。

步骤是：先将两个样本合并，定义入样指标 $R_i$；然后拟合一个倾向得分模型——通常是 logistic 回归；最后，对非概率样本中的每个单元，用其入样概率的倒数作为权重，计算加权平均。

这里的逻辑是：如果一个单元入样概率很小，说明它在非概率样本中被"低估"了，应该给它更大的权重。

但同样有一个关键假设：倾向得分模型必须正确设定。如果真实的入样机制包含非线性项、交互项，而我们只拟合了线性的 logistic 模型，IPW 就是不一致的。权重算错了，整个估计也就偏了。

---

## 第6页：方法三——双重稳健估计 DR

第三种方法是双重稳健估计，它结合了前面两种方法。

公式看起来比较复杂，但含义很清楚。第一项是在非概率样本上，用逆概率权重对残差 $y_i - m(x_i, \hat\beta)$ 进行加权；第二项是在概率样本上，用设计权重对预测值 $m(x_i, \hat\beta)$ 进行加权。

它的核心性质是双重稳健性：只要结果模型和倾向得分模型中有一个是正确的，DR 估计量就是一致的。不需要两个都对。这比单独使用 REG 或 IPW 有明显的改进。

但是，这里有一个重要的"但是"：当两个模型都错误设定的时候——比如结果模型是线性的但真实关系是非线性的，同时倾向得分模型是 logistic 线性的但真实入样机制也是非线性的——那么 DR 估计量也会不一致。参数化的双重稳健，不等于非参数保护。

---

## 第7页：文章的核心思路

这篇文章的核心创新点在这里。

传统的倾向得分模型假设 $\operatorname{logit}[\pi^A(x)]$ 是协变量的线性函数，即 $\alpha_0 + x^T\alpha$。但真实的选择机制可能是完全未知的非线性函数。

文章的方案是：把这个参数模型替换成一个非参数模型——用一个深度神经网络来逼近未知函数 $g_0(x)$。整个流程是：协变量 $x$ 输入 → DNN 输出 $\hat g(x)$ → 通过 logistic 变换得到入样概率 $\hat\pi^A(x)$ → 再代入 IPW 或 DR 框架，得到 DIPW 和 DDR 估计量。

DNN 的训练同时利用了非概率样本和概率样本的信息，这是一个关键特点。

---

## 第8页：为什么用深度神经网络？

为什么选择 DNN 而不是其他非参数方法？有四个理由。

第一，万能逼近性。多层前馈网络可以逼近紧集上非常广泛的连续函数类。

第二，维数灾难的规避。在复合 Hölder 光滑性假设下，稀疏 DNN 可以达到最优的 minimax 收敛速度，而且不会遭受维数灾难——收敛速度取决于函数的内在维度，而不是协变量的原始维度。

第三，可扩展性。相比核平滑等经典非参数方法，当目标函数具有合适的结构时，DNN 在中等或较高维度下表现得更好。

第四，也是最关键的：非概率样本的入样机制恰恰是一个未知的、可能存在交互项和非线性的函数。这正是 DNN 擅长处理的场景。

值得强调的是，参数 logit 线性模型其实是 DNN 模型的一个特例——不设隐藏层或者令激活函数为恒等映射即可退化回去。DNN 的优势在于让数据自己"学"出函数形式，而不是事先强加一个线性结构。

---

## 第9-10页：伪似然函数的推导

接下来是最核心的方法论部分——DNN 倾向得分的估计。这里的关键是如何构造一个可用于训练 DNN 的损失函数。

**第一步（不可行的总体似然）**：如果我们能观测到总体中所有单元的协变量 $x_i$，那么入样指标 $R_i$ 的似然函数可以直接写出。利用 logit 变换，总体对数似然可以写成两个部分之和。

**第二步（用 HT 估计量代替总体总和）**：但在现实中，我们只有两个样本的协变量，总体的协变量是未知的。好在概率样本 $S_B$ 是从总体中按已知概率抽取的，我们可以用 Horvitz-Thompson 估计量来估计总体总和。具体来说，就是用 $\sum_{i\in S_B} d_i^B \log(1-\pi_i^A)$ 来代替 $\sum_{i=1}^N \log(1-\pi_i^A)$。

**第三步（代入非参数 logit 模型）**：在非参数模型 $\log[\pi_i^A/(1-\pi_i^A)] = g(x_i)$ 下，伪对数似然化简为一个非常漂亮的形式：$\ell^*(g) = \sum_{i\in S_A} g(x_i) - \sum_{i\in S_B} d_i^B \log\{1+\exp[g(x_i)]\}$。

第一项只用了非概率样本，第二项用了概率样本作为总体的代替。这个伪似然函数是理解整篇论文方法的钥匙。

---

## 第11页：DNN 架构与训练

估计出 $g_0$ 之后，入样概率就可以通过 logistic 变换得到：$\hat\pi_i^A = \{1+\exp[-\hat g(x_i)]\}^{-1}$。

下面介绍 DNN 的具体架构。采用 $(K+1)$ 层的前馈网络，宽度向量为 $\mathbf{p}$。输入维度 $p_0=r$ 等于协变量的维数，输出维度 $p_{K+1}=1$ 为标量 logit 值。$K$ 个隐藏层使用 ReLU 激活函数：$\sigma(u) = \max\{0, u\}$。

网络类定义为稀疏网络类 $\mathcal{G}(K,s,\mathbf{p},D)$：非零参数总数不超过 $s$，函数的 sup 范数不超过 $D$。稀疏性约束有助于防止过拟合，同时支持理论分析。

训练方面，损失函数是负的伪对数似然 $-\ell^*(g)$，优化器采用 ADAM——一种自适应矩估计的随机梯度下降变体，能够自适应地调整学习率。

---

## 第12页：提出的估计量

训练好 DNN 之后，我们就得到了两个新的估计量。

**DIPW（DNN-assisted IPW）**：用 DNN 估计的入样概率 $\hat\pi_i^A$ 代替参数 logistic 回归估计的概率，构造逆概率加权估计。形式上与传统 IPW 相同，只是倾向得分的来源变了。

**DDR（Deep Doubly Robust）**：将 DNN 倾向得分嵌入双重稳健框架。第一项是逆概率加权的残差，第二项是概率样本上结果模型预测的加权平均。

DDR 继承了传统 DR 的结构——当结果模型正确时保持一致性——同时因为倾向得分是用 DNN 非参数估计的，对倾向得分模型的误设具有更强的稳健性。

---

## 第13-15页：假设条件

理论分析需要一些假设条件，我们分组来看。

**识别条件 A1-A3**：
- A1（可忽略性）：$R_i$ 与 $y_i$ 在给定 $x_i$ 条件下独立。这是缺失随机（MAR）假设在调查抽样中的版本。
- A2（正值性）：每个总体单元的入样概率都大于零。A1 和 A2 一起构成了 Rosenbaum 和 Rubin (1983) 提出的强可忽略条件。
- A3（条件独立性）：不同单元的入样指标在给定协变量后相互独立。这排除了整群抽样或网络依赖等复杂选择结构。

**DNN 复杂度条件 B1-B2**：
- B1：网络的深度 $K$、宽度 $p_k$ 和稀疏度 $s$ 需要随样本量适当增长。深度足够以逼近 $g_0$，但稀疏度受控以避免过拟合。
- B2：真实的 logit 入样函数 $g_0$ 属于复合 Hölder 光滑类。这是 DNN 能以 $\gamma_n$ 速度逼近 $g_0$ 的根本原因。

**正则条件 C1-C9** 包括设计一致性、矩条件、logistic 链接函数的光滑性、DNN 类上的一致设计一致性、以及估计得分截断等。这些条件在非参数和调查抽样的交叉领域中都是标准的。

---

## 第16页：定理一——DNN 估计量的收敛速度

现在进入理论结果。

$g_0$ 属于复合 Hölder 光滑类 $\mathcal{H}(q,\boldsymbol{\beta},\mathbf{d},\tilde{\mathbf{d}},M)$，意味着 $g_0$ 可以表示为 $q$ 个函数的复合，每个分量具有光滑度 $\beta_i$ 和内在维度 $\tilde{d}_i \le d_i$。

整体的收敛速度 $\gamma_n$ 由这些分量中的"瓶颈"决定。

定理一：在假设 A1-A3、B1-B2 和条件 C1、C8 下，DNN 估计量 $\hat g$ 对 $g_0$ 的 $L^2$ 收敛速度为 $O_p(\gamma_n \log^2 n)$。

关键洞见：收敛速度取决于**内在维度** $\tilde{d}_i$，而不是原始协变量的维度 $r$。当内在维度远小于名义维度时，DNN 可以有效规避维数灾难。这是 DNN 相对于核方法等经典非参数方法的核心理论优势。

---

## 第17页：定理二——DIPW 和 DDR 的收敛速度

定理二将 DNN 的收敛速度传递到最终的均值估计量。

在所有假设条件下（包括 $g_0$ 的正确设定），DIPW 和 DDR 都达到相同的收敛速度：$|\hat\mu_{DIPW} - \mu_y|$ 和 $|\hat\mu_{DDR} - \mu_y|$ 都是 $O_p(\gamma_n \log^2 n)$。

这个结论的意义在于：DIPW 和 DDR 都继承了 DNN 的非参数收敛速度。速度由 $g_0$ 的复合光滑度和内在维度决定，而非协变量个数。这为使用 DNN 处理非线性入样机制提供了严格的理论支撑。

---

## 第18页：模拟研究

理论分析之外，模拟研究也提供了重要的有限样本证据。

总体规模 $N=20000$，非概率样本 $n_A=500$，概率样本 $n_B=1000$。真实的入样概率包含了交互项、平方项、正弦项和对数项——这是一个典型的非线性入样机制。

考虑了两种设定场景：
- **TF 场景**：结果模型正确设定，参数倾向得分模型误设。此时 DIPW 相比传统 IPW 有明显改善；DDR 的偏差和均方误都最小。
- **FF 场景**：两个参数工作模型都误设。REG、IPW 和传统 DR 的表现都恶化，而 DIPW 和 DDR 保持了相对稳定。

但需要注意：FF 场景下的好表现是本次模拟设计下的有限样本证据，并不是任意误设下的一般性保证。

---

## 第19页：实证应用

论文还将方法应用于真实数据。

非概率数据来自 2015 年 Pew Research Center 的调查样本，$n_A=9301$；概率参考数据来自 2015 年 BRFSS，$n_B=441456$。共同协变量包括年龄、性别、种族、地区、婚姻状况、就业和教育。结果变量包括邻里信任、政治参与、地方投票和饮酒天数等。

实证结果发现：调整后的估计量与原始的非概率样本均值有所差异，说明概率样本改变了目标总体的对准。但由于真实总体均值未知，实证应用主要用来说明方法的可实施性和估计量之间的比较，而不是对准确性的最终判断。

---

## 第20页：总结

让我做一个简短的总结。

这篇论文的核心贡献是：在非概率样本和概率样本整合的框架下，用深度神经网络代替参数化的倾向得分模型，构建了 DIPW 和 DDR 两个估计量。

理论上，文章在正则条件下建立了 DNN 估计量和最终均值估计量的一致性和收敛速度。速度由目标函数的复合光滑度和内在维度决定，而非简单的协变量个数。

模拟研究表明，当真入样机制为非线性时，DDR 和 DIPW 相比传统参数方法有显著改进，尤其在两个参数模型都误设的严峻场景下。

方法的一个自然扩展是：同时对结果回归模型也使用 DNN 进行非参数建模，构建完全非参数的双重稳健估计。这将进一步提高对模型误设的稳健性。

我的报告就到这里，谢谢大家，欢迎提问。